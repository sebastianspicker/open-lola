# Open LoLa Final Plan Remediation Ledger

Source of truth: `final-plan.md` (2026-05-21).

Status vocabulary: `NOT_STARTED`, `IN_PROGRESS`, `BLOCKED`, `DEFERRED`,
`IMPLEMENTED`, `VERIFIED`, `COMPLETE`.

This ledger tracks bounded remediation slices derived only from `final-plan.md`.
Each row must move to `VERIFIED` or `COMPLETE` only with command evidence in the
`Tests` and `Verification` columns.

| Slice ID | Title | Severity | Findings | Status | Files changed | Tests | Verification | Result | Blocker/uncertainty |
|---|---|---|---|---|---|---|---|---|---|
| PH0-BASELINE | Establish pre-change baseline | P1 verification blocker | Phase 0 baseline gate | COMPLETE | `plan-remediation-ledger.md`, `plan-remediation-status.md` | `swift test --no-parallel` | 2026-05-22: initial run failed before code edits on missing companion files; after companions and S-P0-001, rerun passed 870 tests after refreshing the stale `/private/tmp/open-lola2-swiftpm-build/debug/open-lola` helper binary. | Baseline and companion-document gate are now established. | Initial baseline was not green until the requested companion files existed. |
| PH0-UDP-HARNESS | UDP concurrent close and blocking receive harness | P0 verification blocker | P0-002, P1-TEST-006 | COMPLETE | `Tests/OpenLolaCoreTests/UdpMediaTransportTests.swift` | `swift test --filter udpMediaTransportCloseCompletesWhileReceiveIsBlocking` | Before fix: failed because `close()` took 946266 us while `receive()` was blocking; after fix: passed. | Focused loopback harness reproduces the lock/close stall and guards the fix. | Thread Sanitizer runtime is unavailable on this Mac; see S-P0-002. |
| PH0-IOPROC-HARNESS | IOProc quiescence harness | P0 verification blocker | P0-001, P1-TEST-007 | COMPLETE | `Tests/OpenLolaCoreTests/DirectPeerRealtimeAudioGraphLifecycleTests.swift`, `Tests/OpenLolaCoreTests/DirectPeerRealtimeAudioGraphTests.swift` | `swift test --filter directPeerRealtimeAudioGraphStopWaitsForActiveCallbackBeforeDestroyingIOProc` | Before fix: failed because `stop()` returned while callback was active and destroy observed active callback; after fix: passed. | Focused harness proves the lifecycle race at source level and guards the fix. | Hardware stress and ASan remain unavailable unless explicitly run outside this slice. |
| PH0-P2P-CONNECT-HARNESS | Partial connect rollback harness | P1 verification blocker | P1-P2P-005, P1-TEST-009 | COMPLETE | `Tests/OpenLolaCoreTests/PeerSessionRunnerTests.swift` | `swift test --filter peerSessionRunnerStartMediaCleansUpOnPartialConnectFailure` | Before fix: failed because `audioTransport.isClosed` remained false after video connect threw; after fix: passed. | Focused harness proves partial media startup closes already-connected transports and does not increment media-start metrics. | Uses a negotiated invalid video host to force the second connect to fail without adding a mock transport abstraction. |
| PH0-UI-METRICS-HARNESS | Zero-value latency hero metrics harness | P1 verification blocker | P1-UI-003, P1-TEST-008 | COMPLETE | `Tests/OpenLolaCoreTests/AppLatencyHeroMetricsTests.swift` | `swift test --filter appLatencyHeroMetricsMakeIgnoresZeroValuedMeasurements` | Before fix: failed because zero jitter and latency were returned as `0.0`; after fix: passed. | Focused harness proves zero-valued timing measurements are absent rather than rendered as measured green hero metrics. | Test lives in a dedicated file to keep `AppShellBehaviorTests.swift` within the line-budget gate. |
| S-P0-001 | Core Audio IOProc lifecycle quiescence | P0 | P0-001 | VERIFIED | `Sources/OpenLolaCore/Audio/Realtime/DirectPeerRealtimeAudioGraph.swift`, `Sources/OpenLolaCore/Audio/Realtime/DirectPeerRealtimeAudioGraphCleanup.swift`, `Sources/OpenLolaCore/Audio/Realtime/DirectPeerRealtimeAudioGraphCallbacks.swift`, `Tests/OpenLolaCoreTests/DirectPeerRealtimeAudioGraphLifecycleTests.swift`, `Tests/OpenLolaCoreTests/DirectPeerRealtimeAudioGraphTests.swift` | `swift test --filter directPeerRealtimeAudioGraphStopWaitsForActiveCallbackBeforeDestroyingIOProc`; `swift test --filter DirectPeerRealtimeAudioGraphTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | `swift build` and product-scoped helper build both failed under SwiftPM sandbox and passed outside sandbox; full Swift suite passed 870 tests after refreshing stale helper binary. | Added active IOProc callback accounting; `stop()` now stops devices, marks callbacks closed, waits for 10ms stable quiescence within a 100ms bound, then destroys IOProc IDs. | Source-level verified. Not field-proven until real CoreAudio start/stop stress with AddressSanitizer runs. |
| S-P0-002 | UDP transport lock and close concurrency | P0 | P0-002 | VERIFIED | `Sources/OpenLolaCore/Network/UDP/UdpMediaTransport.swift`, `Sources/OpenLolaCore/Network/UDP/UdpMediaTransportLifecycle.swift`, `Tests/OpenLolaCoreTests/UdpMediaTransportTests.swift` | `swift test --filter udpMediaTransportCloseCompletesWhileReceiveIsBlocking`; `swift test --filter UdpMediaTransportTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | Focused test passes after the fix; UDP transport suite passes 9 tests; line-budget gate passes; full non-parallel Swift suite passes 871 tests. `swift test --sanitize=thread --filter udpMediaTransportCloseCompletesWhileReceiveIsBlocking` failed before test execution both inside and outside the sandbox because dyld rejected Xcode's TSAN runtime with `Sanitizer load violates platform policy`. | `receive()` no longer holds `stateLock` across the blocking datagram receive; `close()` marks the socket closed under lock, interrupts a blocked receive with `shutdown`, then closes the descriptor. | Required TSAN verification is blocked by local Xcode/macOS runtime policy and must be rerun after the sanitizer runtime can load. |
| S-P1-MADI-001 | Correct MADI output device UID handoff | P1 | P1-MADI-001 | VERIFIED | `Sources/OpenLolaCore/Audio/MADI/MadiFullDuplexRuntime.swift`, `Tests/OpenLolaCoreTests/MadiFullDuplexSessionTests.swift` | `swift test --filter madiFullDuplexHandoffConfigurationPreservesSplitDeviceUIDs`; `swift test --filter MadiFullDuplexSessionTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | Focused test failed before the final fix because `outputDeviceUID` was `"rme-madi-input"`; after the fix, the focused test, MADI session suite, line-budget gate, and full non-parallel Swift suite passed 874 tests after refreshing the product-scoped CLI helper. `bash scripts/verify-docs.sh` and `git diff --check` passed. | `MadiFullDuplexSession.handoffConfiguration` now takes explicit input and output device UIDs and passes the output UID into `RealtimeAudioEngineConfiguration.outputDeviceUID`. | Source/test verified. Manual proof with two distinct CoreAudio device UIDs was not run on this machine. |
| S-P1-MEDIA-001 | MediaClock overflow safety | P1 | P1-MEDIA-001, P2-TEST-004 | COMPLETE | `Sources/OpenLolaCore/Timing/MediaClock.swift`, `Tests/OpenLolaCoreTests/MediaClockTests.swift` | `swift test --filter mediaClockNanosecondsClampsOverflowInsteadOfTrapping`; `swift test --filter MediaClockTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | Before fix, the focused overflow test exited with signal 5 at `MediaClock.nanoseconds overflow`; after fix, the focused test, MediaClock suite, line-budget gate, and full non-parallel Swift suite passed 875 tests. `swift build` failed inside sandbox and passed outside sandbox; the product-scoped helper build passed outside sandbox before the broad suite. `bash scripts/verify-docs.sh` and `git diff --check` passed. | `MediaClock.nanoseconds(forFrameCount:sampleRateHertz:)` now clamps full-width overflow and final half-up rounding overflow to `UInt64.max` instead of trapping. | `P1-RXBUF-001` remains a separate RxBuffer arithmetic slice. |
| S-P1-RT-001 | Stop failure cleanup state | P1 | P1-RT-001 | VERIFIED | `Sources/OpenLolaCore/Audio/Realtime/DirectPeerRealtimeAudioGraph.swift`, `Tests/OpenLolaCoreTests/DirectPeerRealtimeAudioGraphTests.swift` | `swift test --filter directPeerRealtimeAudioGraphFailedStopClearsRunningAndDestroysIOProc`; `swift test --filter DirectPeerRealtimeAudioGraphTests`; `swift test --filter DirectPeerRealtimeAudioGraphLifecycleTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | Before fix, the focused failed-stop regression failed because no destroy call occurred and injected capture still saw the graph as running. After fix, the focused test, realtime graph suite, lifecycle quiescence suite, line-budget gate, and full non-parallel Swift suite passed 876 tests. `swift build` failed inside the SwiftPM sandbox and passed outside it; the product-scoped helper build passed outside the sandbox before the broad suite. `bash scripts/verify-docs.sh` and `git diff --check` passed. | `stopUnlocked()` now clears `ioProcRunning` for any stop cleanup attempt, destroys existing IOProcs even when `AudioDeviceStop` reports failure, and retains only IOProc IDs whose destroy call actually failed for retry. | Source/test verified with injected CoreAudio stop failure. No real-device stop-failure run was performed. |
| S-P1-UDP-001 | TCP control partial-write retry | P1 | P1-UDP-001 | COMPLETE | `Sources/OpenLolaCore/Connectors/LoLa/LoLaTcpControlExchangeRuntime.swift`, `Tests/OpenLolaCoreTests/LoLaCompatibilityTcpControlTests.swift` | `swift test --filter lolaTcpSendRetriesPartialWritesUntilControlDatagramIsComplete`; `swift test --filter LoLaCompatibilityTcpControlTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | Before fix, the focused short-write regression failed with `socketFailed("tcp send")` after an injected 128-byte TCP send. After fix, the focused test, TCP control suite, line-budget gate, and full non-parallel Swift suite passed 877 tests. `swift build` failed inside the SwiftPM sandbox and passed outside it; the product-scoped helper build passed outside the sandbox before the broad suite. | TCP control sends now use a write-all loop over `Darwin.send`, advancing the buffer offset until the full LoLa control datagram is sent, retrying `EINTR`, and preserving hard send failures. | Uses an injected send closure for deterministic short-write proof plus existing loopback TCP control coverage. |
| S-P1-P2P-004 | Error transition from recovery | P1 | P1-P2P-004 | COMPLETE | `Tests/OpenLolaCoreTests/PeerSessionRunnerLifecycleTests.swift` | `swift test --filter peerSessionErrorDuringRecoveryFailsSessionAndClosesMediaTransports`; `swift test --filter PeerSessionRunnerLifecycleTests`; `swift test --filter SessionProtocolTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | The focused recovery-error regression passed before any production change, proving current source already allows `.error` from recovering/paused control state. PeerSessionRunner lifecycle suite, session protocol suite, line-budget gate, and full non-parallel Swift suite passed 878 tests. `swift build` failed inside the SwiftPM sandbox and passed outside it. `bash scripts/verify-docs.sh` and `git diff --check` passed. | Current source already transitions a fatal, current-session `.error` during recovery to `.failed`, increments the media stop boundary, and closes media transports; the new regression pins that behavior. | No production code changed because the finding was stale against current source. Does not address transcript growth or peer-confirmed running-state slices. |
| S-P1-P2P-002 | Bound control transcript growth | P1 | P1-P2P-002 | COMPLETE | `Sources/OpenLolaCore/Network/P2P/PeerSessionRunner.swift`, `Tests/OpenLolaCoreTests/PeerSessionRunnerLifecycleTests.swift` | `swift test --filter peerSessionRunnerControlTranscriptIsBoundedUnderHighFrequencyMessages`; `swift test --filter PeerSessionRunnerLifecycleTests`; `swift test --filter PeerSessionRunnerTests`; `swift test --filter SessionProtocolTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | Before fix, the focused transcript regression failed because the transcript reached 1203 entries and retained handshake/proposal entries. After fix, the focused test, lifecycle suite, PeerSessionRunner suite, session protocol suite, line-budget gate, and full non-parallel Swift suite passed 879 tests. `swift build` failed inside the SwiftPM sandbox and passed outside it; the product-scoped helper build passed outside the sandbox before the broad suite. `bash scripts/verify-docs.sh` and `git diff --check` passed. | `controlTranscript` is capped at the last 1000 sent control messages, while the last sent proposal is stored separately so delayed accept validation does not depend on retained transcript history. | This keeps the existing sent-message transcript model; it does not add received-message history or address the separate peer-confirmed running-state slice. |
| S-P1-UI-003 | Zero latency/jitter does not produce green hero state | P1 | P1-UI-003 | COMPLETE | `Sources/open-lola-app/AppLatencyHeroMetrics.swift`, `Tests/OpenLolaCoreTests/AppLatencyHeroMetricsTests.swift` | `swift test --filter appLatencyHeroMetricsMakeIgnoresZeroValuedMeasurements`; `swift test --filter AppShellBehaviorTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | Focused test, AppShellBehavior suite, line-budget gate, and full non-parallel Swift suite passed 873 tests. | `AppLatencyHeroMetrics.make()` now ignores non-positive jitter and fastest-AV latency values before exposing hero metrics. | Does not address separate UI findings for live state derivation or topology animation. |
| S-P1-UI-004 | Remove unsupported debug menu labels | P1 | P1-UI-004 | COMPLETE | `Sources/open-lola-app/OpenLolaApp.swift`, `Tests/OpenLolaCoreTests/AppMenuRenderingTests.swift`, `Tests/OpenLolaCoreTests/AppShellSlice05Tests.swift` | `swift test --filter appMenuSourceDoesNotRenderUnsupportedDebugLabels`; `swift test --filter appMenuRenderingFiltersFutureUnmappedActions`; `swift test --filter AppMenuRenderingTests`; `swift test --filter AppShellSlice05Tests`; `swift test --filter AppShellTransportMenuPolicyTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel`; `bash script/build_and_run.sh --verify` | Before fix, the focused source-policy test failed because app source still contained `Unsupported:` and `unsupportedMenuAction`. After fix, the focused tests, menu rendering suite, app shell slice suite, transport menu policy suite, line-budget gate, and full non-parallel Swift suite passed 881 tests. `swift build` and app-bundle verification failed inside the SwiftPM sandbox and passed outside it; app verification produced `dist/app-launch-evidence`. `bash scripts/verify-docs.sh` and `git diff --check` passed. | Unsupported or future unmapped menu actions are filtered before SwiftUI command rendering; no disabled `Unsupported:` debug item is exposed in the production menu, and DEBUG builds log omitted action IDs. | This removes unsupported debug labels without adding missing shortcuts; P2-UI-013 remains separate if selected from the source audit. |
| S-P1-MADI-002 | CoreAudio string ownership fix | P1 | P1-MADI-002 | VERIFIED | `Sources/OpenLolaCore/Audio/CoreAudio/CoreAudioInventoryReader.swift`, `Tests/OpenLolaCoreTests/CoreAudioInventoryTests.swift` | `swift test --filter coreAudioInventoryReaderRestrictsRetainedStringPropertiesToDocumentedSelectors`; `swift test --filter CoreAudioInventoryTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel`; `.build/debug/open-lola device-inventory` | Before fix, the focused ownership-boundary test failed to compile because no explicit retained-selector policy existed. After fix, the focused test, CoreAudio inventory suite, line-budget gate, and full non-parallel Swift suite passed 882 tests. `swift build` and product-scoped helper build failed inside the SwiftPM sandbox and passed outside it. `swift test --sanitize=address --filter CoreAudioInventoryTests` built but failed before execution because dyld rejected the ASan runtime with `Sanitizer load violates platform policy`. Live device inventory failed inside sandbox with `noDevices` and passed outside sandbox against built-in CoreAudio devices. `bash scripts/verify-docs.sh` and `git diff --check` passed. | `stringProperty` is now `retainedStringProperty` and only uses `takeRetainedValue()` for CoreAudio CFString selectors documented by `AudioHardwareBase.h` as caller-owned: object name, manufacturer, and device UID. Unsupported future selectors return nil instead of entering the retained path. | Source/test verified and live non-sanitized inventory-smoked. ASan real-device proof is blocked by local sanitizer runtime policy; no RME MADI hardware inventory was run. |
| S-P1-RXBUF-001 | RxBufferPolicy overflow-safe factories | P1 | P1-RXBUF-001 | COMPLETE | `Sources/OpenLolaCore/Timing/RxBuffering.swift`, `Tests/OpenLolaCoreTests/RxBufferingTests.swift` | `swift test --filter rxBufferPolicyFactoriesRejectOverflowingPacketFrameProducts`; `swift test --filter RxBufferingTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | Before fix, the focused overflow regression exited with signal 5 when factory multiplication trapped. After fix, the focused test, RxBuffering suite, line-budget gate, and full non-parallel Swift suite passed 883 tests. `swift build` and product-scoped helper build failed inside the SwiftPM sandbox and passed outside it. `bash scripts/verify-docs.sh` and `git diff --check` passed. | `RxBufferPolicy` factory methods now multiply packet counts by frames per packet through checked arithmetic and return `.arithmeticOverflow(...)` field errors instead of trapping or producing wrapped frame targets. | Pure arithmetic slice; no external hardware proof is required. |
| S-P1-P2P-005 | Partial media connect rollback | P1 | P1-P2P-005 | COMPLETE | `Sources/OpenLolaCore/Network/P2P/PeerSessionRunner.swift`, `Tests/OpenLolaCoreTests/PeerSessionRunnerTests.swift` | `swift test --filter peerSessionRunnerStartMediaCleansUpOnPartialConnectFailure`; `swift test --filter PeerSessionRunnerTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | Product helper build failed inside the SwiftPM sandbox and passed outside the sandbox; focused test, PeerSessionRunner suite, line-budget gate, and full non-parallel Swift suite passed 872 tests. | `startMedia()` now tracks successfully connected media transports, closes them on later startup failure, marks the runner failed, and rethrows the original error. | Does not address other P1-P2P findings such as peer-confirmed running state, recovery error transition, or transcript growth. |
| S-P1-P2P-001 | Peer-confirmed running state | P1 | P1-P2P-001 | COMPLETE | `Sources/OpenLolaCore/Network/P2P/PeerSessionRunner.swift`, `Sources/OpenLolaCore/Network/P2P/PeerSessionRunnerLoopbackPair.swift`, `Tests/OpenLolaCoreTests/PeerSessionRunnerLifecycleTests.swift`, `Tests/OpenLolaCoreTests/PeerSessionAVSupportTests.swift` | `swift test --filter peerSessionRunnerDoesNotReportRunningBeforePeerMediaStart`; `swift test --filter peerSessionRunnerDoesNotReportRunningWhenPeerMediaStartArrivesBeforeLocalStart`; `swift test --filter PeerSessionRunnerLifecycleTests`; `swift test --filter PeerSessionRunnerTests`; `swift test --filter ReconnectionTests`; `swift test --filter PeerSessionMetricsAndControlTests`; `swift test --filter PeerSessionAVSupportTests`; `swift test --filter DirectPeerSessionAVQualityPolicyTests`; `swift test --filter DirectPeerSessionAVRXBufferProfileTests`; `swift test --filter SessionProtocolTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel` | Before fix, the focused peer-confirmed-start regression failed because both peers reported `.running` immediately and audio send succeeded before a peer media-start message was received. After fix, the focused ordering tests, lifecycle suite, P2P runner suite, reconnection suite, metrics/control suite, AV support suites, session protocol suite, line-budget gate, and full non-parallel Swift suite passed 885 tests. `swift build` and product-scoped helper build failed inside the SwiftPM sandbox and passed outside it. `bash scripts/verify-docs.sh` and `git diff --check` passed. | `PeerSessionRunner` now keeps public lifecycle state at `.mediaStarting` after local media setup and transitions to `.running` only after a current-session peer `.mediaStart` is received, while preserving peer-first ordering by remembering a prior peer start. Loopback helpers now exchange media-start control messages explicitly before tests send media. | Source/test verified through loopback control exchange. No multi-machine live P2P run was performed. |
| S-P1-MADI-003 | Audio device capability cross-check | P1 | P1-MADI-003 | VERIFIED | `Sources/OpenLolaCore/Audio/Routing/DirectAudioMediaRouter.swift`, `Sources/OpenLolaCore/Network/P2P/PeerSessionRunner.swift`, `Tests/OpenLolaCoreTests/DirectAudioMediaRouterTests.swift` | `swift test --filter DirectAudioMediaRouterTests`; `swift test --filter PeerSessionRunnerTests`; `swift test --filter PeerSessionRunnerLifecycleTests`; `swift test --filter PeerSessionAVSupportTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel`; `bash scripts/verify-docs.sh`; `git diff --check` | Before fix, the focused router capability regression failed to compile because no structured router error or capability-aware initializer existed. After fix, the focused router suite, nearby P2P runner/lifecycle/AV suites, line-budget gate, and full non-parallel Swift suite passed 886 tests. `swift build` and product-scoped helper build failed inside the SwiftPM sandbox and passed outside it. Docs verification and diff hygiene passed after tracker updates. | `DirectAudioMediaRouter` now validates raw PCM stream construction against the local audio capability set for supported payload, sample rate, frames-per-packet, sample format, channel count, and channel indices before constructing `MadiReceiveEngine`; `PeerSessionRunner.startMedia()` passes its local capabilities into the router. Non-PCM audio streams are skipped by the raw PCM router so Opus/AES67 sessions are not rejected by a receiver they do not use. | Source/test verified against advertised local capabilities. No physical CoreAudio device/RME inventory proof was run in this slice. |
| S-P2-MADI-001 | MADI peer readiness before socket streaming | P2 | P2-MADI-001 | VERIFIED | `Sources/OpenLolaCore/Audio/MADI/MadiFullDuplexSocketRunner.swift`, `Sources/OpenLolaCore/Audio/MADI/MadiFullDuplexTypes.swift`, `Tests/OpenLolaCoreTests/MadiFullDuplexSessionTests.swift` | `swift test --filter MadiFullDuplexSessionTests`; final `swift test --no-parallel` passed 901 tests | `swift test --filter CodeLineBudgetTests`; `swift build --product open-lola`; `swift build --product open-lola-app`; `swift build --product open-lola --build-path /private/tmp/open-lola2-swiftpm-build`; `bash scripts/verify-docs.sh`; `git diff --check` all passed after this slice | `MadiFullDuplexSocketRunner` now exchanges bounded UDP readiness datagrams tied to session ID and peer ID before entering the media packet loop. A lone peer times out with `peerReadinessTimeout` instead of streaming optimistically, and the existing two-peer loopback run still exchanges media after readiness. | Source/test verified only. This is socket-level readiness, not physical two-peer RME MADI hardware proof. |
| S-P1-RT-002 | Deinit avoids audio lifecycle lock deadlock | P1 | P1-RT-002 | VERIFIED | `Sources/OpenLolaCore/Audio/Realtime/DirectPeerRealtimeAudioGraph.swift`, `Tests/OpenLolaCoreTests/DirectPeerRealtimeAudioGraphLifecycleTests.swift` | `swift test --filter directPeerRealtimeAudioGraphDeinitDoesNotAcquireLifecycleLock`; `swift test --filter DirectPeerRealtimeAudioGraphLifecycleTests`; `swift test --filter DirectPeerRealtimeAudioGraphTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel`; `bash scripts/verify-docs.sh`; `git diff --check` | Before fix, the focused deinit regression failed because releasing the last graph reference timed out while the lifecycle lock was held. After fix, the focused regression, lifecycle suite, realtime graph suite, line-budget gate, and full non-parallel Swift suite passed 887 tests. `swift build` and product-scoped helper build failed inside the SwiftPM sandbox and passed outside it. Docs verification and diff hygiene passed after tracker updates. | `DirectPeerRealtimeAudioGraph.deinit` now uses the existing `stopUnlocked()` cleanup path instead of public `stop()`, avoiding lifecycle-lock acquisition during deinit while preserving bounded IOProc quiescence, IOProc destroy, and cleanup-result behavior before scratch buffers are deallocated. | Source/test verified with DEBUG cleanup hooks and a lock-contention regression. No real CoreAudio teardown stress or sanitizer run was performed for this slice. |
| S-P1-RT-003 | Remove syscall timing from RT callback | P1 | P1-RT-003 | VERIFIED | `Sources/OpenLolaCore/Audio/Realtime/DirectPeerRealtimeAudioGraph.swift`, `Tests/OpenLolaCoreTests/DirectPeerRealtimeAudioGraphTimingTests.swift` | `swift test --filter directPeerRealtimeAudioGraphCallbackTimingAvoidsDispatchTimeNowSource`; `swift test --filter DirectPeerRealtimeAudioGraphTimingTests`; `swift test --filter DirectPeerRealtimeAudioGraphTests`; `swift test --filter DirectPeerRealtimeAudioGraphLifecycleTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel`; `bash scripts/verify-docs.sh`; `git diff --check` | Before fix, the focused source guard failed because `DirectPeerRealtimeAudioGraph.swift` still contained `DispatchTime.now()` and did not contain `mach_absolute_time()`. After fix, the focused timing suite, realtime graph suite, lifecycle suite, line-budget gate, and full non-parallel Swift suite passed 889 tests. `swift build` and product-scoped helper build failed inside the SwiftPM sandbox and passed outside it. Docs verification and diff hygiene passed after tracker updates. | Callback duration timing now uses `mach_absolute_time()` and converts Mach tick deltas through the precomputed timebase; DEBUG tick injection proves callback duration and deadline-miss counters remain meaningful without `DispatchTime.now()` source in the graph. | Source/test verified only. No real CoreAudio jitter/stress/sanitizer proof was run, and `P1-RT-004` remains a separate per-sample validation-loop slice. |
| S-P1-RT-004 | Remove per-sample guard loop from RT callback | P1 | P1-RT-004 | VERIFIED | `Sources/OpenLolaCore/Audio/Realtime/DirectPeerRealtimeAudioGraph.swift`, `Sources/OpenLolaCore/Audio/Realtime/DirectPeerRealtimeAudioGraphBufferCopy.swift`, `Tests/OpenLolaCoreTests/DirectPeerRealtimeAudioGraphTimingTests.swift` | `swift test --filter directPeerRealtimeAudioGraphCallbackCopyAvoidsPerSampleOffsetGuardSource`; `swift test --filter DirectPeerRealtimeAudioGraphTimingTests`; `swift test --filter DirectPeerRealtimeAudioGraphTests`; `swift test --filter DirectPeerRealtimeAudioGraphLifecycleTests`; `swift test --filter CodeLineBudgetTests`; `swift test --no-parallel`; `bash scripts/verify-docs.sh`; `git diff --check` | Before fix, the focused source guard failed because `DirectPeerRealtimeAudioGraph.swift` still called `audioByteOffset(` and did not contain `audioChannelCopyPlan(`. After fix, the focused timing/copy suite, realtime graph suite, lifecycle suite, line-budget gate, and full non-parallel Swift suite passed 891 tests. `swift build` and product-scoped helper build failed inside the SwiftPM sandbox and passed outside it. Docs verification and diff hygiene passed after tracker updates. | The callback copy path now validates each mapped audio channel once per callback, then copies frame bytes by advancing prevalidated offsets. Planar valid-copy behavior and malformed short-buffer drops are covered without adding allocation or blocking work to the callback path. | Source/test verified only. `final-plan.md` names `RealtimeAudioBuffers.swift`, but the live per-sample AudioBufferList validation loop was in `DirectPeerRealtimeAudioGraph.swift`; no real CoreAudio jitter/stress/sanitizer proof was run. |
| S-P1-SLOP-001-002 | Remove synthetic zero and todo evidence | P1 | P1-SLOP-001, P1-SLOP-002 | VERIFIED | `Sources/OpenLolaCore/Support/SourceValidationMetrics.swift`, deleted `Sources/OpenLolaCore/Support/SyntheticPlaceholderMetrics.swift`, synthetic/report surfaces under `Sources/OpenLolaCore`, `scripts/verify-release-readiness.sh`, `Tests/OpenLolaCoreTests/ProductionEvidencePlaceholderPolicyTests.swift`, report contract tests | `swift test --filter productionSourcesDoNotShipSyntheticPlaceholderMetricsOrManualTodoEvidence`; `swift test --filter SyntheticSmokeReportContractTests`; `swift test --filter LatencyBenchmarkReportTests`; `swift test --filter E2EBenchmarkReportTests`; `swift test --filter HardwareValidationReportTests`; `swift test --filter IntegratedProfileReportTests`; `swift test --no-parallel` | `swift build`; source grep for `SyntheticPlaceholderMetrics\|todo\(human\)` under production `Sources/`; `bash scripts/verify-docs.sh`; `shellcheck -x scripts/verify-release-readiness.sh`; `git diff --check`; `bash scripts/verify-release-readiness.sh` | Production `Sources/` no longer contain `SyntheticPlaceholderMetrics` or literal `todo(human)`; the zero-placeholder helper is deleted; release readiness now has a production source grep gate and completes with `source-gate-verdict: pass`, `product-runtime-verdict: partial`, `VERDICT: PARTIAL`. | Source/test verified only. `SourceValidationMetrics` are deterministic source-validation values, not physical field measurements; reports remain `PARTIAL` unless external measured evidence proves otherwise. |
| S-P1-UI-001 | Live UI state backed by runtime evidence or removed | P1 | P1-UI-001 | VERIFIED | `Sources/open-lola-app/AppDesignSystem.swift`, `Sources/open-lola-app/AppSessionStateBanner.swift`, `Sources/open-lola-app/AppConnectionTopologyView.swift`, `Sources/open-lola-app/AppPreviewReceiverView.swift`, `Sources/open-lola-app/AppShellRootView.swift`, `Sources/open-lola-app/AppTransportView.swift`, app-shell policy tests | `swift test --filter appSessionStateSurfaceDoesNotExposeUnbackedLiveState`; `swift test --filter AppShell`; `swift test --filter CodeLineBudgetTests` | Before fix, the focused state-surface test failed because `AppSessionState.allCases` still exposed `"Live"`. After fix, the focused test passed, `swift test --filter AppShell` passed 90 tests, and the line-budget gate passed. `swift build --product open-lola-app` failed inside the SwiftPM sandbox with `sandbox-exec: sandbox_apply: Operation not permitted` and passed outside the sandbox. `bash scripts/verify-docs.sh` and `git diff --check` passed after tracker updates. | Removed the unbacked `.live` session state and dead live-navigation/stop-confirmation paths. Banner copy now distinguishes running, awaiting evidence, and completed evidence; topology peer/arrow labels remain muted instead of promoting validated post-run evidence to active live flow. | Source/test verified only. No live packet/RMS evidence source was added; `S-P1-UI-002` remains responsible for animation tied to packet evidence. |
| S-P1-UI-002 | Topology animation tied to packet evidence | P1 | P1-UI-002 | VERIFIED | `Sources/open-lola-app/AppConnectionTopologyView.swift`, `Sources/open-lola-app/AppShellSectionViews.swift`, `Tests/OpenLolaCoreTests/AppShellUIPolicyTests.swift` | `swift test --filter appConnectionTopologyAnimationRequiresPacketEvidence`; `swift test --filter AppShell`; `swift test --filter CodeLineBudgetTests` | Before fix, the focused animation policy test failed because `.supervisorRunning` alone returned true. After fix, the focused test passed, `swift test --filter AppShell` passed 90 tests, and the line-budget gate passed. `swift build --product open-lola-app` failed inside the SwiftPM sandbox with `sandbox-exec: sandbox_apply: Operation not permitted` and passed outside the sandbox. `bash scripts/verify-docs.sh` and `git diff --check` passed after tracker updates. | Topology flow animation now requires `.supervisorRunning`, reduced motion disabled, and decoded packet evidence from a capture report with packet count greater than zero. A bare supervisor process start no longer animates media flow. | Source/test verified only. Existing capture reports are completed packet evidence, not a live packet/RMS stream; future live evidence can feed the same explicit gate. |
| S-P2-UI-012 | Evidence incomplete is warning, not green | P2 | P2-UI-012 | VERIFIED | `Sources/open-lola-app/AppValidationConsoleModels.swift`, `Sources/open-lola-app/AppShellSectionViews.swift`, `Tests/OpenLolaCoreTests/AppShellBehaviorTests.swift` | `swift test --filter appValidationPreflightReportsBlockersWithTargetSections`; `swift test --filter AppShell`; `swift test --filter CodeLineBudgetTests` | Before fix, the focused preflight regression failed because `.evidenceIncomplete` resolved to the ready/green tone. After fix, the focused test passed, `swift test --filter AppShell` passed 90 tests, and the line-budget gate passed. `swift build --product open-lola-app` failed inside the SwiftPM sandbox with `sandbox-exec: sandbox_apply: Operation not permitted` and passed outside the sandbox. `bash scripts/verify-docs.sh` and `git diff --check` passed after tracker updates. | Validation preflight tone mapping is now explicit and testable; `.evidenceIncomplete` maps to warning/amber instead of ready/green. | Source/test verified. This is UI tone mapping only; it does not change validation verdict semantics. |
| S-P2-UI-001 | Replace fragile fail substring tone mapping | P2 | P2-UI-001 | VERIFIED | `Sources/open-lola-app/AppTransportView.swift`, `Tests/OpenLolaCoreTests/AppShellTransportMenuPolicyTests.swift` | `swift test --filter appTransportStatusToneUsesTypedExecutionPhase`; `swift test --filter AppShell`; `swift test --filter CodeLineBudgetTests` | Before fix, the focused transport-tone regression failed because `.failedToStart` and `.validationFailed` remained secondary while idle text containing `"failed"` turned red. After fix, the focused test passed, `swift test --filter AppShell` passed 91 tests, and the line-budget gate passed. `rg` found no `"fail"` substring matching in `AppTransportView.swift`. `swift build --product open-lola-app` failed inside the SwiftPM sandbox with `sandbox-exec: sandbox_apply: Operation not permitted` and passed outside the sandbox. `bash scripts/verify-docs.sh` and `git diff --check` passed after tracker updates. | Transport status badge tone now uses typed `AppExecutionPhase` state: running is connecting, failed-to-start/run-failed/validation-failed are error, and other idle text is neutral. | Source/test verified. This changes status badge color policy only, not execution state transitions. |
| S-P2-UI-007 | Add key VoiceOver state announcements | P2 | P2-UI-007 | VERIFIED | `Sources/open-lola-app/AppSessionStateBanner.swift`, `Tests/OpenLolaCoreTests/AppShellTransportMenuPolicyTests.swift` | `swift test --filter appSessionBannerAccessibilityAnnouncementTargetsKeyStateTransitions`; `swift test --filter AppShell`; `swift test --filter CodeLineBudgetTests` | Before fix, the focused announcement regression failed because `.armed`, `.supervisorRunning`, and `.validated` returned nil. After fix, the focused test passed, `swift test --filter AppShell` passed 91 tests, and the line-budget gate passed. `swift build --product open-lola-app` failed inside the SwiftPM sandbox with `sandbox-exec: sandbox_apply: Operation not permitted` and passed outside the sandbox. `bash scripts/verify-docs.sh` and `git diff --check` passed after tracker updates. | Session banner accessibility announcements now cover key operator state transitions: armed, supervisor running, evidence validated, preview warning, and error. Routine states such as ready remain silent to avoid announcement noise. | Source/test verified. No manual VoiceOver run was performed. |
| S-P2-UI-008 | Replace internal SSH settings help text | P2 | P2-UI-008 | VERIFIED | `Sources/open-lola-app/AppShellSettingsTabs.swift`, `Tests/OpenLolaCoreTests/AppShellTransportMenuPolicyTests.swift` | `swift test --filter appExecutionModeUnavailableHelpUsesOperatorFacingCopy`; `swift test --filter AppShell`; `swift test --filter CodeLineBudgetTests` | Before fix, the focused copy regression failed because the settings help text contained `"runtime fallback contract"` and `"orchestration"`. After fix, the focused test passed, `swift test --filter AppShell` passed 92 tests, and the line-budget gate passed. `swift build --product open-lola-app` failed inside the SwiftPM sandbox with `sandbox-exec: sandbox_apply: Operation not permitted` and passed outside the sandbox. `bash scripts/verify-docs.sh` and `git diff --check` passed after tracker updates. | SSH settings unavailable help now uses operator-facing language: SSH launch is unavailable in Settings, Local execution is supported here, and operators can copy an SSH supervisor command from operator artifacts. | Copy-only UI slice; no SSH runtime behavior changed. |
| S-P2-UI-011 | Surface preview window request state | P2 | P2-UI-011 | VERIFIED | none | `swift test --filter appPreviewWindowRequestFeedbackDoesNotClaimDisplaySuccess`; `swift test --filter AppShell`; `swift test --filter CodeLineBudgetTests` | The focused preview-window feedback test passed before production changes, proving current source already reports a request-sent, not-confirmed state and does not claim the window opened. `swift test --filter AppShell` passed 92 tests, and the line-budget gate passed. `swift build --product open-lola-app` failed inside the SwiftPM sandbox with `sandbox-exec: sandbox_apply: Operation not permitted` and passed outside the sandbox. | Current source already sets `previewState.receiverStatus` before requesting the Local Preview window and uses menu help that says display success is not confirmed. | No production code changed because the finding is already addressed in current source. No manual window-display smoke was performed. |
| S-P2-DEAD-001 | Verify and decide UltraGrid dead-code path | P2 | P2-DEAD-001 | VERIFIED | none | `swift test --filter externalConnectorLaunchPlansCoverUltraGridJackTripAndAvTransportPorts`; `swift test --filter ExternalConnectorSessionTests`; `.build/debug/open-lola external-connector-session-run --connector mvtp-ultragrid --role tx --peer 127.0.0.1 --output /private/tmp/open-lola-ultragrid-dead-code-evidence.json --dry-run true` | Current source contradicts the dead-code finding: UltraGrid/MVTP is wired through `external-connector-session-run`, parser defaults and connector-specific flags, `ExternalConnectorSessionRunner`, reference-peer parity scripts, current docs, and tests. Focused launch-plan test passed; `ExternalConnectorSessionTests` passed 10 tests; the rebuilt CLI dry-run wrote a PARTIAL `mvtpUltraGrid` report. `swift build --product open-lola` failed inside the SwiftPM sandbox with `sandbox-exec: sandbox_apply: Operation not permitted` and passed outside the sandbox before the CLI dry-run. | UltraGrid connector is active source-level comparison/runtime surface and should not be deleted. | No production code changed. No live UltraGrid peer interoperability run was performed. |
| S-P2-DEAD-002 | Verify and decide JackTrip dead-code path | P2 | P2-DEAD-002 | VERIFIED | none | `swift test --filter externalConnectorLaunchPlansCoverUltraGridJackTripAndAvTransportPorts`; `swift test --filter ExternalConnectorSessionTests`; `.build/debug/open-lola external-connector-session-run --connector jacktrip --role tx --peer 127.0.0.1 --peer-audio-port 4464 --output /private/tmp/open-lola-jacktrip-dead-code-evidence.json --dry-run true` | Current source contradicts the dead-code finding: JackTrip is wired through `external-connector-session-run`, parser defaults and connector-specific flags, `ExternalConnectorSessionRunner`, reference-peer parity scripts, current docs, app contract copy, and tests. Focused launch-plan test passed; `ExternalConnectorSessionTests` passed 10 tests; the CLI dry-run wrote a PARTIAL `jackTrip` report after requiring the JackTrip-specific peer audio port. | JackTrip connector is active source-level comparison/runtime surface and should not be deleted. | No production code changed. No live JackTrip reference-peer interoperability run was performed. |
| S-P2-DEPR-001 | Replace UltraGrid file-scope `try!` | P2 | P2-DEPR-001 | VERIFIED | `Sources/OpenLolaCore/Connectors/UltraGrid/UltraGridCompatibility.swift`, `Tests/OpenLolaCoreTests/UltraGridCompatibilityTests.swift` | `swift test --filter ultraGridRawVideoFourCCConstantsAvoidThrowingFileScopeInitialization`; `swift test --filter UltraGrid`; `swift test --filter CodeLineBudgetTests`; `swift build --product open-lola` | Before fix, the focused regression failed because `UltraGridCompatibility.swift` contained file-scope `try! UltraGridFourCC` constants. After fix, the focused regression passed, `swift test --filter UltraGrid` passed 37 tests, and the line-budget gate passed. `swift build --product open-lola` failed inside the SwiftPM sandbox with `sandbox-exec: sandbox_apply: Operation not permitted` and passed outside the sandbox. | Raw-video FourCC constants now use non-throwing raw values for `RGB3` and `RGBA`, preserving 24-bit and 32-bit UltraGrid video packetization without a module-initialization force-try crash surface. | Source/test verified only. `UltraGridProtocolModel.swift` still contains a separate pre-existing file-scope `try!` for `UltraGridRTPPayloadRegistry.default`; it was observed but left out of this slice because `final-plan.md` scoped P2-DEPR-001 to `UltraGridCompatibility.swift`. |
| S-P2-DEDUP-001 | Consolidate validator boilerplate | P2 | P2-DEDUP-001 | VERIFIED | subsystem `*Validators.swift` files under `Audio`, `Benchmarks`, `Evidence`, `Release`, `Timing`, and `Video`; deleted `Sources/OpenLolaCore/Audio/MADI/MadiFullDuplexValidation.swift`; scattered report-helper files that previously carried typealias-only validator declarations; `Tests/OpenLolaCoreTests/ValidationPrimitivesTests.swift` | `swift test --filter reportPrimitiveValidatorTypealiasOnlyDeclarationsLiveInSubsystemValidatorFiles`; `swift test --filter ValidationPrimitivesTests`; `swift test --no-parallel`; `swift test --filter CodeLineBudgetTests`; `bash scripts/verify-docs.sh`; `git diff --check` | Before consolidation, the focused source-inventory regression failed with 29 typealias-only validator declarations outside `*Validators.swift`. After consolidation, the focused regression passed, `ValidationPrimitivesTests` passed 4 tests, and the full non-parallel Swift suite passed 896 tests after refreshing the product-scoped helper binary. Docs verification and diff hygiene passed after tracker updates. | The 29 exact typealias-only `ReportPrimitiveValidating` validators now live in subsystem `*Validators.swift` files; custom validators with behavior remain colocated with their reports. | Source/test verified. The first broad suite run failed because `/private/tmp/open-lola2-swiftpm-build/debug/open-lola` was stale; `swift build --product open-lola --build-path /private/tmp/open-lola2-swiftpm-build` failed inside the SwiftPM sandbox and passed outside the sandbox before the rerun. No manual runtime smoke was performed. |
| S-P2-DEDUP-002 | Decide connector parallel file structures | P2 | P2-DEDUP-002 | DEFERRED | none | not run | `find Sources/OpenLolaCore/Connectors -maxdepth 2 -type f`; `rg -n "P2-DEDUP-002\|LaunchPlan\|PassValidation\|ProtocolModel\|AudioPayloadCodec" final-plan.md Sources/OpenLolaCore/Connectors Tests/OpenLolaCoreTests docs`; `bash scripts/verify-docs.sh`; `git diff --check` | Inspection confirmed the parallel connector files are active source-level runtime/comparison surfaces after S-P2-DEAD-001 and S-P2-DEAD-002 kept UltraGrid/MVTP and JackTrip active. `final-plan.md` explicitly says to defer this connector-structure cleanup, and a cross-connector abstraction would touch high-risk protocol-specific launch, validation, topology, media, and evidence code. Docs verification and diff hygiene passed after tracker updates. | Deferred as not safely actionable in the current slice without a separate connector-specific design plan and behavior-preserving migration. | No production code changed. Future work should start with an explicit connector architecture plan and tests for each affected protocol surface. |
| S-P2-SLOP-001 | Inline single MADI channel count constant | P2 | P2-SLOP-001 | VERIFIED | `Sources/OpenLolaCore/Audio/MADI/MadiTransmit.swift`; deleted `Sources/OpenLolaCore/Audio/MADI/MadiChannelCounts.swift` | `swift test --filter MadiTransmit`; `swift test --filter MadiReceive`; `swift test --filter CodeLineBudgetTests`; `bash scripts/verify-docs.sh`; `git diff --check` | Source checks confirmed `MadiChannelCounts.swift` no longer exists and the remaining `madiSyntheticRequiredChannelCounts` definition lives in `MadiTransmit.swift`. MADI transmit tests passed 3 tests, MADI receive tests passed 7 tests, line-budget gate passed, docs verification passed, and diff hygiene passed. | Removed the one-line MADI channel-count file by moving the synthetic required-channel matrix into the MADI transmit/report source area while preserving TX/RX synthetic coverage behavior. | Source/test verified only. No manual MADI runtime or hardware smoke was performed because this slice only moves an internal synthetic matrix constant. |
| S-P2-SLOP-002 | Verify UdpPcmLoopbackDefaults production use | P2 | P2-SLOP-002 | VERIFIED | `Sources/OpenLolaCore/Network/UDP/UdpPcmLoopbackSmokes.swift`; deleted `Sources/OpenLolaCore/Network/UDP/UdpPcmLoopbackDefaults.swift` | `swift test --filter UdpPcmLoopbackLatencyTests`; `swift test --filter SyntheticSmokeReportContractTests`; `swift test --filter NatFriendlyRouteTests`; `swift test --filter CodeLineBudgetTests`; `bash scripts/verify-docs.sh`; `git diff --check` | Source search showed `UdpPcmLoopbackDefaults` was used only by `UdpPcmLoopbackSmokes.swift`, which is still production source for synthetic loopback and NAT route smokes. After moving the constants into that file as a private enum and deleting the standalone file, loopback tests passed 8 tests, synthetic smoke tests passed 3 tests, NAT route tests passed 7 tests, line-budget gate passed, and docs/diff checks passed after tracker updates. | Removed the one-purpose defaults file while keeping the defaults private to the production smoke code that still consumes them. No runtime loopback CLI defaults were changed. | Source/test verified only. The enum was not moved to test support because production synthetic/NAT smoke code still uses it. No live network route smoke was run for this file-local cleanup. |
| S-P3-SLOP-003 | Extract repeated version string | P3 | P3-SLOP-003 | VERIFIED | `Sources/OpenLolaCore/Core/OpenLolaCLI.swift`, `Sources/OpenLolaCore/Network/P2P/PeerSessionRunner.swift`, `Tests/OpenLolaCoreTests/SessionProtocolTests.swift` | `swift test --filter directPeerFactoriesUseCLIImplementationVersion`; `swift test --filter SessionProtocolTests`; `swift test --filter PeerSessionRunnerTests`; `swift test --filter CodeLineBudgetTests`; `bash scripts/verify-docs.sh`; `git diff --check` | Source grep confirmed `0.0.0-m06` appears only at `OpenLolaCLI.implementationVersion`. The focused direct-peer factory test passed, SessionProtocolTests passed 6 tests, PeerSessionRunnerTests passed 5 tests, line-budget gate passed, docs verification passed, and diff hygiene passed. | Direct P2P capability/factory surfaces now read the M06 implementation version from one `OpenLolaCLI.implementationVersion` constant instead of repeating the same string literal. | Source/test verified only. This is an internal constant consolidation and does not change the advertised implementation version. |
| S-P2-STRUCT-001 | Validate NetworkRouteCommandMatrix paths | P2 | P2-STRUCT-001 | VERIFIED | `Tests/OpenLolaCoreTests/NetworkRouteCommandMatrixTests.swift` | `swift test --filter networkRouteCommandMatrixEntriesHaveExistingOwnersSourcesAndTests`; `swift test --filter NetworkRouteCommandMatrixTests`; `swift test --filter CodeLineBudgetTests`; `bash scripts/verify-docs.sh`; `git diff --check` | The focused path-inventory test passed after adding representative-command path extraction. The first full matrix suite run failed only because `/private/tmp/open-lola2-swiftpm-build/debug/open-lola` was stale; `swift build --product open-lola --build-path /private/tmp/open-lola2-swiftpm-build` failed inside the SwiftPM sandbox and passed outside the sandbox, then `NetworkRouteCommandMatrixTests` passed 6 tests. Docs verification and diff hygiene passed after tracker updates. | The matrix now validates owner source files, related source/test files, and repo-relative paths embedded in representative command strings. | Source/test verified only. This adds a path guard for the existing matrix; it does not restructure the docs-as-code command matrix. |
| S-P2-STRUCT-002 | Validate SourceOwnershipInventory paths | P2 | P2-STRUCT-002 | VERIFIED | `Tests/OpenLolaCoreTests/SourceOwnershipInventoryTests.swift` | `swift test --filter SourceOwnershipInventoryTests`; `swift test --filter CodeLineBudgetTests`; `bash scripts/verify-docs.sh`; `git diff --check` | The baseline ownership inventory suite already covered current source paths, related tests, fixtures, and docs. The tightened path guard now also extracts repo-relative paths embedded in `validationCommands`; after the change, `SourceOwnershipInventoryTests` passed 4 tests, the line-budget gate passed, and docs/diff checks passed after tracker updates. | Source ownership inventory validation now covers active current paths plus repo-relative command-path references, preventing stale command script/doc/source path strings from silently surviving. | Source/test verified only. `proposedSourcePath` remains a future-placement hint and is not required to exist as a current repo path. |
| S-P2-STRUCT-004 | Document OpenLolaContracts alias intent | P2 | P2-STRUCT-004 | VERIFIED | `Sources/OpenLolaCore/Core/OpenLolaContractsAliases.swift`, `docs/source-contracts.md` | `swift test --filter OpenLolaContractsTargetTests`; `swift test --filter CodeLineBudgetTests`; `bash scripts/verify-docs.sh`; `git diff --check` | Existing tests already prove the `OpenLolaContracts` target is framework-free and `OpenLolaCore` re-exports extracted contracts for current callers. The slice documents the coupling intent in source and public source contracts. Focused contract tests passed 2 tests, line-budget gate passed, docs verification passed, and diff hygiene passed. | `OpenLolaContracts` is now documented as the canonical module for shared framework-free report contracts, while `OpenLolaContractsAliases.swift` is explicitly a small source-compatibility alias set for existing `OpenLolaCore` callers. | Documentation/source-comment verified only. No alias behavior or public contract type changed. |
| S-P2-STRUCT-003 | Split DirectPeerSessionReport validation if it grows | P2 | P2-STRUCT-003 | DEFERRED | none | not run | not run | Deferred by `final-plan.md`: split only if file grows further. | No current growth trigger in this slice. |
| S-P3-POLISH | Optional P3 polish bundle | P3 | P3-UDP-001, P3-RXBUF-001, P3-P2P-001, P3-MADI-001, P3-OVER-001, P3-OVER-002, P3-DEPR-001, P3-SLOP-001, P3-SLOP-002, P3-SLOP-003, P3-UI-001, P3-UI-002, P3-UI-003 | VERIFIED | `Sources/OpenLolaCore/Connectors/LoLa/LoLaCompatibilityWireFrame.swift`, `Sources/OpenLolaCore/Audio/MADI/MadiReceive.swift`, `Sources/OpenLolaCore/Audio/MADI/MadiReceiveBuffers.swift`, `Sources/OpenLolaCore/Network/P2P/PeerSessionRunner.swift`, `Sources/OpenLolaCore/Support/SPSCAtomicRing.swift`, `Sources/OpenLolaCore/Network/UDP/NetworkByteReader.swift`, `Sources/OpenLolaCore/Network/P2P/DirectPeerFNV1A.swift`, `Sources/open-lola-app/AppDesignSystem.swift`, `Sources/open-lola-app/AppPreviewReceiverView.swift`, `Tests/OpenLolaCoreTests/ExternalConnectorLoLaCompatibilityTests.swift`, `Tests/OpenLolaCoreTests/MadiReceiveTests.swift`, `Tests/OpenLolaCoreTests/PeerSessionRunnerTests.swift`, `Tests/OpenLolaCoreTests/SPSCAtomicRingTests.swift`, `Tests/OpenLolaCoreTests/AppShellUIPolicyTests.swift`, `Tests/OpenLolaCoreTests/NetworkByteReaderTests.swift` | `swift test --filter lolaWireFrameRoundTripsRecoveredEnvelopePaddingAndVariableIPv4IDs`; `swift test --filter ExternalConnectorLoLaCompatibilityTests`; `swift test --filter MadiReceiveTests`; `swift test --filter PeerSessionRunnerTests`; `swift test --filter SPSCAtomicRingTests`; `swift test --filter AppShellUIPolicyTests`; `swift test --filter NetworkByteReaderTests`; `swift test --filter Udp`; `swift test --filter LoLaCompatibility`; `swift test --filter JackTripCompatibilityTests`; `swift test --filter UltraGridCompatibilityTests`; `swift test --filter AES67ST2110L24TransportTests`; `swift test --filter DirectPeerTwoPeerPrototypeReportTests`; `swift test --filter RxBufferingTests`; `swift test --filter PlaceholderDetectionTests`; final `swift test --no-parallel` passed 900 tests after refreshing the product-scoped CLI helper | `rg -n "AppSessionState\\.live|PlaceholderDetection|PlaceholderSensitiveField|AppChannelMeter"` inspections; source inspection of realtime/MADI Rx buffer overrun handling; `swift build --product open-lola` and `swift build --product open-lola-app` passed outside the SwiftPM sandbox; `bash scripts/verify-docs.sh` passed; `git diff --check` passed | Non-zero LoLa trailing bytes are rejected while zero Ethernet padding remains accepted; MADI buffer constructors return typed capacity errors; P2P shutdown clears stale session state; SPSC owner violations now return runtime invalid results and count violations outside DEBUG; `NetworkByteReader` has a central readable-range guard; FNV-1a hot-path/non-crypto intent is documented; preview output copy now distinguishes selected Blackmagic inventory from generic local-preview fallback; stale/no-delete P3 items were verified. | P3-RXBUF-001 is deferred as a separate runtime-policy design issue: current direct graph/MADI paths already drop/flag overruns and feed adaptive pressure, while adding generic corrective action would alter latency behavior. No physical hardware/manual runtime proof was run for this optional polish bundle. |
