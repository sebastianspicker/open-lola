# Open LoLa plan.md Verification Ledger

Date: 2026-05-22

Verdict: `FALSIFIED`. The current tree does not prove that every issue in
`plan.md` has been addressed.

Scope:
- Source of truth checked here: `plan.md`.
- Existing remediation evidence checked: `plan-remediation-ledger.md` and
  `plan-remediation-status.md`.
- Live source/test checks used `rg`, focused file reads, and the latest
  recorded verification commands in the remediation trackers.

Important limitation:
- `plan-remediation-ledger.md` explicitly tracks bounded slices derived from
  `final-plan.md`, not every section-qualified issue in `plan.md`. It is strong
  evidence for many implemented fixes, but it is not a complete proof that all
  `plan.md` findings are closed.

Status vocabulary:
- `ADDRESSED`: current source/test evidence covers the plan issue at source
  level.
- `PARTIAL`: some source/test remediation exists, but the original plan issue
  is not fully closed.
- `OPEN`: live source/test evidence still matches the plan issue.
- `DEFERRED`: intentionally postponed or unsafe without a separate design.
- `EXTERNAL_BLOCKED`: requires hardware, sanitizer runtime, or live peer proof
  not available in this checkout.
- `NOT_VERIFIED`: this pass did not find enough evidence to call the issue
  addressed.

## Addressed Summary

The existing remediation ledger gives source/test evidence for several high-risk
items, including Core Audio IOProc quiescence, UDP close-vs-receive concurrency,
MADI output UID handoff, MediaClock/RxBuffer overflow handling, LoLa TCP
partial-write retry, peer-confirmed P2P running state, P2P transcript bounds,
partial media-connect rollback, validation tone mapping, `.live` state removal,
topology animation gating on packet evidence, synthetic placeholder cleanup,
UltraGrid/JackTrip active-use decisions, validator boilerplate consolidation,
MADI peer readiness, SPSC owner violations, LoLa trailing-byte rejection, and
MADI buffer typed capacity errors.

Those rows remain source/test evidence only where the tracker says hardware,
sanitizer, or live-peer proof was unavailable.

## Open And Partial Ledger

| Ledger ID | plan.md issue(s) | Severity | Status | Evidence from current tree | Why not closed | Required closure |
|---|---|---:|---|---|---|---|
| PVL-COVERAGE-001 | All `plan.md` issues | P1 | ADDRESSED | `plan-verification-ledger.md` enumerates the remaining section-qualified `plan.md` misses, and `plan-missed-remediation-ledger.md` maps them to PMR remediation rows. | Coverage gap closed by companion ledgers; individual PMR rows still carry their own open/partial/external status. | Keep both companion ledgers current until all PMR rows are closed or explicitly archived. |
| PVL-RT-001 | Section 9 `RT-AUDIO-002` | P2 | ADDRESSED | `RealtimeAudioPacketHandoffRuntime` still contains an `NSLock`, but it is now documented as host-thread-only and `realtimeAudioPacketHandoffRuntimeLockIsNotUsedByProductionRealtimeSources` proves current production sources do not reference the locked wrapper. `RealtimeAudioPacketHandoffTests` passed. | Closed for current source ownership; the wrapper itself is not claimed realtime-safe. | Keep the source ownership test if production handoff owners change, or replace the wrapper with a bounded nonblocking primitive before using it on an IOProc path. |
| PVL-RT-002 | Section 7 `RT-AUDIO-004` | P2 | ADDRESSED | `RealtimeAudioBufferListReader` now guards invalid indexes and unavailable `mBuffers` offsets, and `realtimeAudioCaptureRingBufferListAccessAvoidsForceUnwrapAndTrapSource` passed. | Closed for source/test scope. | Keep source-policy test in place; no external proof required. |
| PVL-RT-003 | Section 7 `RT-AUDIO-005` | P2 | ADDRESSED | `RealtimeAudioFixedTargetJitterBuffer` records `packetAccountingUnderflows`, clamps count, marks hidden playout growth, and `fixedTargetJitterBufferReportsAccountingUnderflowWithoutCrash` passed. | Closed for source/test scope. | Keep regression in the fixed-target jitter-buffer suite. |
| PVL-RT-004 | Pass 3 `RT-AUDIO-005`, P0 Core Audio IOProc lifecycle proof | P0 | EXTERNAL_BLOCKED | `plan-remediation-status.md` states CoreAudio lifecycle is source/test verified but not field-proven; ASan/hardware stress was not run. | Source-level harnesses do not prove real CoreAudio callback quiescence on hardware. | Run documented real CoreAudio start/stop stress and sanitizer proof once the local sanitizer runtime can load. |
| PVL-RT-005 | Pass 3 callback-duration harness, Pass 6 `TEST-005` | P2 | ADDRESSED | `directPeerRealtimeAudioGraphCallbackTimingCountersCoverMaxChannelFrameShape` now uses injected callback ticks and asserts `callbackMaxMicroseconds` stays under the buffer-period threshold with no deadline miss. | Closed for deterministic test scope; hardware profiling remains a separate external readiness concern. | Keep deterministic timing hook coverage and run hardware profiling only for release evidence. |
| PVL-UDP-001 | `UDP-NET-002`, Pass 3 control replay/sequence concerns | P2 | ADDRESSED | `docs/p2p-networking.md` documents the v1 policy: no generic control sequence number, session-scoped commands are state/session gated, simultaneous proposals fail closed, and this is not cryptographic anti-replay. `controlReplayPolicyIsDocumentedAsSessionBoundAndIdempotent` passed. | Closed by explicit documented omission plus state-machine tests, not by adding a public schema sequence field. | Preserve the documented v1 boundary unless a future protocol version intentionally adds authenticated control sequencing. |
| PVL-UDP-002 | Pass 6 `TEST-004` | P2 | ADDRESSED | `udpMediaTransportReportsNonZeroJitterAfterControlledTransitVariation` sends 20 packets through loopback `UdpMediaTransport` with controlled timestamp variation and asserts non-zero transport jitter. | Closed for source/test scope. | Keep transport-level jitter integration coverage in the UDP filter. |
| PVL-P2P-001 | `P2P-003` | P2 | ADDRESSED | `PeerSessionRunner.shutdown(reason:)` is now non-throwing, matching its synchronous close/record behavior. Production P2P cleanup calls no longer use `try? shutdown`, and `peerSessionShutdownCleanupDoesNotSilentlyDiscardProductionP2PErrors` scans `Sources/OpenLolaCore/Network/P2P` for reintroduced swallowed shutdown cleanup. | Closed for current source/test scope; shutdown cleanup has no throwing failure path to hide. | Keep the production-source scan if shutdown grows a throwing cleanup dependency in the future. |
| PVL-P2P-002 | Pass 3 `P2P-STATE-002`, `CTRL-001`, `CTRL-003` | P1 | ADDRESSED | `peerSessionRejectsSimultaneousProposalWithoutAcceptingEitherSide` proves simultaneous proposals fail closed. `docs/p2p-networking.md` documents serialized `PeerSessionRunner` ownership. `directPeerAVControlDrainStopsOnCurrentShutdownAndIgnoresStaleShutdown` drains stale and current shutdown datagrams in one pass, dropping stale control and stopping on current control. | Closed for current source/test/docs semantics; no new tie-breaker or internal synchronization was added. | Revisit only if a future protocol version requires concurrent runner ownership or proposal tie-breaking. |
| PVL-VIDEO-001 | `VIDEO-001`, `VIDEO-002` | P1 | ADDRESSED | `docs/video-blackmagic-atem.md` keeps DeckLink output and multi-stream production readiness as explicit `PARTIAL` gaps without physical Blackmagic/ATEM evidence. `BlackmagicOutputBoundary` reports `PARTIAL` unless physical DeckLink output evidence exists, report validation rejects Blackmagic/output false PASS, and app preview copy says DeckLink output remains unverified for selected Blackmagic inventory. | Closed for truthful source/docs/UI/report boundary; physical Blackmagic output and multi-camera field readiness remain separate external proof, not source closure. | Run physical Blackmagic/ATEM hardware evidence before any release PASS claim. |
| PVL-VIDEO-002 | `VIDEO-004` | P2 | ADDRESSED | `AVFoundationVideoCaptureRunner.run` now routes start/stop through `AVFoundationCaptureSessionHandle`; `VideoCaptureSessionWorkQueue` dispatches start asynchronously on a dedicated serial queue and serializes stop behind pending start work. `videoCaptureSessionStartDispatchesOffCallerQueueAndSerializesStop` and `VideoCaptureReportTests` passed. | Closed for source/test scope; no live camera smoke was run. | Keep the session work-queue regression and run a live AVFoundation capture smoke before promoting field evidence. |
| PVL-VIDEO-003 | `VIDEO-006`, Pass 6 `TEST-006` | P2 | ADDRESSED | `VideoFrameReassembler` now exposes DEBUG-only receive-time injection for deterministic tests while production callers still use `DispatchTime.now().uptimeNanoseconds`; `videoFrameReassemblerExpiresIncompleteFramesAtProductionDefaultAge` passed and proves default 250 ms expiry at the boundary without sleeps. | Closed for source/test scope. | Keep the production-default expiry regression in the video transport report suite. |
| PVL-RXBUF-001 | `RXBUF-003`, `RXBUF-006`, P3-RXBUF-001 | P2 | ADDRESSED | `AVTimestampAligner.decision` now uses `AVSyncPolicy.staleVideoDropThresholdMicroseconds` before dropping video behind audio; frames behind audio but within the stale threshold render without adding audio delay. `docs/rx-buffering.md` documents that stale-video drops are AV-specific, direct P2P AV derives the threshold from the selected frame interval, and MADI/realtime overruns stay on runtime-specific bounded drop/telemetry policies. `balancedAVUsesStaleVideoThresholdBeforeDroppingBehindAudio` and `AVTimestampAlignmentTests` passed. | Closed for source/test/docs scope; live RX drift/playout proof remains tracked separately in PVL-RXBUF-002. | Keep the AV sync threshold regression and do not add generic RX overrun corrective behavior without a runtime-specific design and tests. |
| PVL-RXBUF-002 | `RXBUF-005`, Pass 3 `RX-001` | P2 | EXTERNAL_BLOCKED | Tracker uncertainty still says direct graph/MADI/P2P runtime proof is source/test only; no real CoreAudio jitter/stress or live RX playout proof was run. | Synthetic/source tests do not prove field RX drift correction and playout behavior. | Run live RX/drift/packet-loss proof with real devices or a deterministic hardware-grade harness. |
| PVL-MADI-001 | `MADI-002` | P2 | ADDRESSED | `docs/audio-rme-madi.md` now records the Core Audio HAL property API compatibility decision: keep `AudioObjectGetPropertyData*` and `AudioObjectSetPropertyData` while the package targets macOS 14, because `AudioHardwareObject` helpers are macOS 15+ in the local SDK. `CoreAudioInventoryReader.swift` and `AudioLoopbackHelpers.swift` carry matching source notes, and `coreAudioHALPropertyAccessDecisionDocumentsMacOS14CompatibilityBoundary` passed. | Closed for source/test/docs scope; live device inventory remains part of separate hardware proof rows. | Revisit when the package deployment target moves to macOS 15+ or a hardware run exposes property-set behavior differences. |
| PVL-MADI-002 | `MADI-001`, `MADI-004`, `MADI-007` hardware proof | P1 | EXTERNAL_BLOCKED | Source/test fixes exist for UID handoff, router capability checks, and socket readiness, but the tracker states no physical two-peer RME MADI hardware proof was run. | The code path is source/test verified only. | Run physical two-peer MADI full-duplex proof with distinct input/output CoreAudio device UIDs. |
| PVL-UI-001 | `UI-NAV-002` | P2 | ADDRESSED | `NativeAppShellActionInventory.menuActions` now declares shortcuts for refresh source/synthetic, arm execution, write two-peer plan, dry run supervisor, validate supervisor report, and open local preview; `AppMenuShortcut` parses the newly declared strings. `appOperatorMenuActionsDeclareSupportedShortcuts`, `appValidationShortcutCopyRequiresMenuContractShortcut`, `AppMenuRenderingTests`, and `AppShellSlice05Tests` passed. | Closed for source/test scope; app menu visual smoke was not run. | Keep contract/parser tests with future menu action changes. |
| PVL-UI-002 | `UI-SESSION-002` | P2 | ADDRESSED | `AppTransportView` now applies help to the transport mode status badge through `AppTransportStatusModePolicy.help`, which returns `sessionMode.unavailableAppReason` for JackTrip and UltraGrid. `appTransportUnavailableConnectorStatusExplainsReason` and `AppShellTransportMenuPolicyTests` passed. | Closed for source/test scope. | Keep status-mode policy coverage with connector availability changes. |
| PVL-UI-003 | `UI-STREAM-002` | P2 | ADDRESSED | `AppPreviewReceiverView` and `AppPreviewSettingsTab` no longer render Return blend, Visible streams, or Selected stream as disabled controls; the surfaces keep non-control status copy through `AppPreviewDisabledReasonCopy.unsupportedLocalPreviewControls`. `appPreviewUnsupportedLocalControlsRenderAsStatusCopyNotInputs`, `AppShellUIPolicyTests`, and `AppShellSlice05Tests` passed. | Closed for source/test scope. | Keep the source regression until preview multi-stream support is intentionally implemented. |
| PVL-UI-004 | `UI-STATE-005` | P2 | ADDRESSED | `AppPreviewReceiverState.previewIsActive` now requires `verifiedPreviewPhase == .active`; `appPreviewReceiverControlsRequireVerifiedActiveServiceState` passed and proves raw preview activity alone does not enable monitor controls. | Closed for source/test scope. | Keep the degraded/partial preview regression in the app shell slice suite. |
| PVL-UI-005 | `UI-SET-002` | P2 | ADDRESSED | `AppShellSettingsSurfacePolicy` and its literal constants were removed from `AppShellSectionViews.swift`; `appSettingsSurfaceDoesNotUseStalePolicyConstants`, `AppShellUIPolicyTests`, and `AppShellSlice05Tests` passed. | Closed for source/test scope. | Keep the source regression until settings surfaces are reorganized. |
| PVL-UI-006 | `UI-PREVIEW-001` | P2 | ADDRESSED | `AppPreviewReceiverState` now tracks preview-window request, visible, and hidden phases; the menu action marks requested before `openWindow(id: "receiver")`, `AppReceiverWindowView` marks visible/hidden from `onAppear`/`onDisappear`, and the main preview routing panel shows `Preview window` status. `appPreviewWindowStateReportsRequestVisibleAndHiddenPhases`, `appPreviewWindowRequestFeedbackDoesNotClaimDisplaySuccess`, and `AppShellTransportMenuPolicyTests` passed. | Closed for source/test scope; no GUI window smoke was run. | Run an app bundle smoke before claiming visual/window behavior on a release candidate. |
| PVL-TEST-001 | Section 11 `TEST-001`, `TEST-002` | P1 | EXTERNAL_BLOCKED | The tracker keeps LoLa, CoreAudio, MADI, P2P, and Blackmagic proof as external/manual gaps. | Synthetic/source tests cannot close findings that require real peer or real device evidence. | Run live LoLa media session and real CoreAudio runtime tests; record artifacts. |
| PVL-TEST-002 | Section 11 `TEST-007`, Pass 6 `TEST-007` | P2 | ADDRESSED | `IntegratedAvNetworkDegradation.apply(impairment:to:)` overlays deterministic `RxImpairmentSimulator` loss/reorder/jitter output onto a measured `VideoTransportReport`, and `integratedAvRunAggregatesDegradedNetworkBeforeAudioImpact` asserts the resulting integrated AV report records video drops/late frames and degradation-before-audio-impact while audio callback/target metrics remain unchanged. | Closed for deterministic source/test scope; it is not live impaired-network field evidence. | Keep this integration test and run real impaired-network field evidence separately before release PASS claims. |
| PVL-TEST-003 | Pass 6 full verification contract | P1 | ADDRESSED | Local verification was rerun after source remediation: `git diff --check`, Swift builds, explicit `/private/tmp/open-lola2-swiftpm-build` CLI build, `swift test --no-parallel` after refreshing that CLI build path, docs verifier, ruff with cache disabled, mypy with cache outside the repo, shellcheck, release hygiene, app bundle verify, and `bash scripts/verify-release-readiness.sh` all completed. The wrapper reported `source-gate-verdict: pass` and `product-runtime-verdict: partial`. | Closed for local source/test/release-readiness verification; product runtime remains `PARTIAL` because Developer ID, notarization, Gatekeeper, clean-Mac, hardware, benchmark, sanitizer, live LoLa/CoreAudio, physical MADI, and RX/drift proof are external/manual gates tracked in other rows. | Keep using cache-disabled or out-of-tree Python verification to avoid `.ruff_cache`/`.mypy_cache` residue, refresh `/private/tmp/open-lola2-swiftpm-build` before CLI-driven Swift tests, and do not promote product runtime beyond `PARTIAL` without the external artifacts. |
| PVL-STRUCT-001 | Pass 4 `DEDUP-002`, `STRUCT-003` | P2 | ADDRESSED | `docs/source-contracts.md` now records the connector/P2P report structure boundary: keep connector families under `Sources/OpenLolaCore/Connectors/`, keep `DirectPeerSessionReport` under `Sources/OpenLolaCore/Network/P2P/`, and require owner/schema/fixture/path/API compatibility proof before a future move. `sourceOwnershipInventoryKeepsConnectorAndP2PReportStructureExplicitUntilDesignedMigration` and `SourceOwnershipInventoryTests` passed. | Closed by explicit no-move design and path ownership tests, not by broad restructuring. | Reopen only if code growth justifies a behavior-preserving migration plan. |

## Conclusion

The repository has addressed the local source/test/design issues found in this
ledger, but the statement "all issues from `plan.md` are addressed" is still
false. The remaining work is external proof: real CoreAudio hardware/sanitizer
stress, live RX drift/playout evidence, physical two-peer MADI proof, live LoLa
media-session/CoreAudio runs, and manual release/runtime artifacts needed before
promoting product runtime beyond `PARTIAL`.
