# Full UltraGrid and JackTrip Runtime Remediation Ledger

Date: 2026-05-18
Plan: `plan.md`
Status: active
Verdict: PARTIAL

This ledger tracks the remaining full-runtime implementation work. It starts
from the current bounded connector baseline and does not mark runtime `PASS`
until measured external peer, live-device, route, timing, and teardown evidence
exists.

| ID | Priority | Runtime | Slice | Status | Evidence required | Verification | Rollback |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RT-00 | P0 | Shared | Lock the evidence contract before enabling `PASS`. | done | Added shared runtime `PASS` evidence classes for reference-peer, live-device, field-route, packet capture, timing, teardown, and media quality evidence. UltraGrid and JackTrip media reports now expose observed and missing runtime `PASS` evidence; later validator slices now reject incomplete `PASS` reports unless every evidence class is present. | `swift test --filter ExternalConnectorReportTests`; `swift test --filter ReportSchemaInventoryTests`; `swift test --filter SyntheticSmokeReportContractTests`; `swift test --filter UltraGridCompatibilityTests`; `swift test --filter JackTripCompatibilityTests` | Revert media-report evidence fields and restore the prior implicit boundary. |
| UG-01 | P1 | UltraGrid | Wire live UltraGrid audio/video providers into the public runner/session path. | done | Public UltraGrid runner now selects structured synthetic, fixture, CoreAudio, and AVFoundation raw8 provider paths; media reports include provider evidence; fixture bytes reach PT21/PT20 packets; live provider selection reports live-device evidence without claiming peer interoperability. | `swift test --filter UltraGridCompatibilityTests`; `swift test --filter ExternalConnectorSessionTests`; `swift test --filter ExternalConnectorAvMatrixTests` | Fall back to explicit synthetic/fixture provider path only. |
| UG-02 | P1 | UltraGrid | Wire UltraGrid playback and render sinks for received media. | done | Received PT21 PCM and reassembled PT20 raw frames are decoded into bounded artifact sink counters, with rejected media counted for malformed audio/video or incomplete reassembly. | `swift test --filter UltraGridCompatibilityTests`; `swift test --filter VideoTransportRunnerTests`; `swift test --filter ExternalConnectorSessionTests` | Retain packet-analysis-only RX behavior. |
| UG-03 | P1 | UltraGrid | Measure direct UltraGrid reference-peer parity in both directions. | blocked on external reference peer host | Measured parity is blocked because `OPEN_LOLA_REFERENCE_PEER_HOST` is not configured in this environment. This blocks interoperability evidence and final `PASS`, not local runtime implementation slices. | `bash scripts/run-reference-peer-parity-gate.sh /private/tmp/open-lola-reference-peer-parity-ultragrid ultragrid` exits 77 with `VERDICT: PARTIAL`; `swift test --filter UltraGridCompatibilityTests` | Keep parity evidence separate from runtime implementation; do not convert skip evidence into parity evidence. |
| UG-04 | P0 | UltraGrid | Add evidence-gated UltraGrid `PASS` validation. | done | UltraGrid media validation now allows `PASS` only when real-link transmission is true, runtime error is absent, all runtime `PASS` evidence classes are observed, and `missingEvidenceClassesForPass` is empty. Synthetic/incomplete PASS reports are rejected, including an invalid synthetic PASS fixture. A valid measured PASS artifact still waits for UG-03 field evidence. | `swift test --filter UltraGridCompatibilityTests`; `swift test --filter ReportFixtureValidationContractTests`; `swift test --filter ExternalConnectorReportTests` | Restore unconditional `dryRunCannotPass` rejection for UltraGrid media reports. |
| UG-05 | P2 | UltraGrid | Add NAT and server-client topology. | done | Added direct-peer and server-client topology configuration, explicit server/client topology state reports, server-side listener behavior without a required peer, connection-plan endpoint commands for local server and remote client roles, and NMP tests proving UltraGrid no longer depends on `uv` preflight as the primary runtime. Field-route and reference-peer evidence remain required before any `PASS` claim. | `swift test --filter UltraGridCompatibilityTests`; `swift test --filter ExternalConnectorConnectionPlanTests`; `swift test --filter ExternalConnectorSessionTests`; `swift test --filter ExternalConnectorNmpPlanTests`; `swift test --filter ExternalConnectorNmpWorkflowTests` | Keep UltraGrid direct-peer only. |
| UG-06A | P2 | UltraGrid | Add dynamic RTP and codec-specific payload negotiation. | done | Added a dynamic RTP payload registry, public UltraGrid audio/video payload-type session flags, TX packetization with configured PT values, RX decode using negotiated mappings, codec-specific rejection for unsupported H.264 mappings, and tests proving implemented PCM/raw-video/JPEG dynamic mappings decode while unmapped dynamic PTs fail loudly. Reference-peer parity remains required before removing the evidence blocker for `PASS`. | `swift test --filter UltraGridCompatibilityTests`; `swift test --filter ExternalConnectorSessionTests`; `swift test --filter ExternalConnectorConnectionPlanTests` | Keep only static PT20/PT21 handling and reject all dynamic RTP payload types. |
| UG-06B | P2 | UltraGrid | Add JPEG mode. | done | Added static PT26 and dynamic `jpeg` RTP/JPEG classification, Swift-native RFC 2435 main JPEG header encode/decode, scan-payload bounds validation, malformed payload rejection tests, and removal of `jpeg` from unsupported UltraGrid modes. Reference-peer JPEG parity remains required before any `PASS` claim. | `swift test --filter UltraGridDynamicRTPTests --no-parallel`; `swift test --filter UltraGridCompatibilityTests --no-parallel` | Restore `jpeg` to unsupported modes and remove PT26/dynamic JPEG decode. |
| UG-06C | P2 | UltraGrid | Add H.264 mode. | done | Added dynamic `h264` RTP classification and a Swift-native RFC 6184 packet validator for single NAL units, STAP-A aggregation, and FU-A fragmentation. Malformed forbidden-bit, unsupported aggregation, and invalid FU-A packets fail loudly. Reference-peer H.264 parity remains required before any `PASS` claim. | `swift test --filter UltraGridDynamicRTPTests --no-parallel`; `swift test --filter UltraGridCompatibilityTests --no-parallel` | Restore `h264` to unsupported modes and remove dynamic H.264 decode. |
| UG-06D | P2 | UltraGrid | Add FEC mode. | done | Added explicit `--ultragrid-fec` configuration, PT22 FEC envelope encode/decode, launch/NMP command propagation, and bounded single-parity video loss recovery for one missing raw-video fragment. Public UltraGrid header evidence covers the PT22 envelope; reference LDGM parity and interoperability evidence remain required before any `PASS` claim. | `swift test --filter UltraGrid`; `swift test --filter ExternalConnectorSessionTests`; `swift test --filter ExternalConnectorConnectionPlanTests` | Restore `fec` to unsupported modes and remove PT22 recovery. |
| UG-06E | P2 | UltraGrid | Add encryption mode. | done | Added PT24/PT25 AES-128-GCM packet wrapping for raw-video and PCM-audio payloads using the public UltraGrid crypto payload header and OpenSSL envelope shape. The runtime now has explicit `--ultragrid-encryption` and redacted passphrase handling, decode rejects encrypted packets without a key, encrypted TX/RX sink counters are covered by tests, and encrypted reference-peer parity remains required before any `PASS` claim. | `swift test --filter UltraGridCompatibilityTests`; `swift test --filter UltraGrid --no-parallel`; `swift test --filter ExternalConnectorSessionTests --no-parallel`; `swift test --filter ExternalConnectorConnectionPlanTests --no-parallel` | Restore `encryption` to unsupported modes and remove PT24/PT25 encryption/decryption. |
| UG-06F | P3 | UltraGrid | Add advanced UltraGrid control APIs. | done | Added a Swift-native UltraGrid TCP control command model from the public control socket source, including CRLF command framing, safe command parsing for stats/mute/volume/av-delay/compress/module messages, launch-plan propagation, media-report control state, and tests proving modeled control commands do not claim reference-peer control-plane success. | `swift test --filter UltraGridCompatibilityTests --no-parallel`; `swift test --filter ExternalConnectorSessionTests --no-parallel` | Restore `advanced-ultragrid-control-apis` to unsupported modes and remove control command/report propagation. |
| JT-01 | P1 | JackTrip | Wire live JackTrip audio providers into the public runner/session path. | done | Public JackTrip runner now selects structured synthetic, fixture, and CoreAudio provider paths; media reports include provider evidence; fixture bytes reach DEFAULT packets; live provider selection reports live-device evidence without claiming peer interoperability. | `swift test --filter JackTripCompatibilityTests`; `swift test --filter ExternalConnectorSessionTests`; `swift test --filter ExternalConnectorAvMatrixTests` | Fall back to explicit synthetic/fixture provider path only. |
| JT-02 | P1 | JackTrip | Wire JackTrip playback and timing sinks for received PCM. | done | Received DEFAULT packets are decoded into bounded interleaved PCM artifact sink counters while stop-control, loss, duplicate, reorder, and redundancy counters remain visible. | `swift test --filter JackTripCompatibilityTests`; `swift test --filter ExternalConnectorSessionTests` | Retain packet-analysis-only RX behavior. |
| JT-03 | P1 | JackTrip | Measure direct JackTrip reference-peer parity in both directions. | blocked on external reference peer host and JackTrip executable | Measured parity is blocked because `OPEN_LOLA_REFERENCE_PEER_HOST` is unset and `jacktrip` is not available on PATH. This blocks interoperability evidence and final `PASS`, not local runtime implementation slices. | `bash scripts/run-reference-peer-parity-gate.sh /private/tmp/open-lola-reference-peer-parity-jacktrip jacktrip` exits 77 with `VERDICT: PARTIAL`; `swift test --filter JackTripCompatibilityTests` | Keep parity evidence separate from runtime implementation; do not convert skip evidence into parity evidence. |
| JT-04 | P2 | JackTrip | Implement 8-bit, 24-bit, and 32-bit PCM DEFAULT audio. | done | JackTrip DEFAULT packet encode/decode now accepts 8/16/24/32-bit header resolutions; runner configuration and parser expose `--jacktrip-bit-resolution`; tests cover planar/interleaved conversion, payload sizing, and packet round trips. | `swift test --filter JackTripCompatibilityTests`; `swift test --filter ExternalConnectorConnectionPlanTests` | Restore non-16-bit PCM rejection. |
| JT-05A | P2 | JackTrip | Add JACK-style local graph backend selection. | done | Public `--jacktrip-audio-backend`/connection-plan selection now accepts `coreaudio` and `jack-graph`; `jack-graph` dry runs produce deterministic local packet evidence without remaining in `unsupportedModes`, while measured JACK graph capture still requires local JACK evidence before any field-readiness claim. | `swift test --filter JackTripCompatibilityTests`; `swift test --filter ExternalConnectorSessionTests`; `swift test --filter ExternalConnectorConnectionPlanTests` | Remove `--jacktrip-audio-backend` and keep implicit CoreAudio/synthetic provider selection only. |
| JT-05B | P2 | JackTrip | Add hub virtual-studio topology. | done | Added explicit direct-peer and hub virtual-studio topology configuration, hub-server/hub-client report state, launch-plan mapping to reference `-S`/`-C`/`-p` mode flags, connection-plan server/client endpoint propagation, and tests proving hub-server audio topology can listen without a configured peer while hub clients require one. Reference-peer route evidence remains required before any `PASS` claim. | `swift test --filter JackTripTopologyTests --no-parallel`; `swift test --filter JackTripCompatibilityTests --no-parallel`; `swift test --filter JackTripPassValidationTests --no-parallel`; `swift test --filter ExternalConnectorConnectionPlanTests --no-parallel` | Restore `hub-virtual-studio` to unsupported modes and remove topology CLI/report propagation. |
| JT-05C | P2 | JackTrip | Add TCP hub handshake. | done | Added a Swift-native unauthenticated hub TCP handshake codec for the public JackTrip port exchange, remote-client-name framing, media-report handshake state, launch-plan/session/connection-plan propagation, malformed-frame rejection, and tests proving auth response codes remain rejected. Reference-peer route evidence remains required before any `PASS` claim. | `swift test --filter JackTripTCPHandshakeTests --no-parallel`; `swift test --filter JackTripCompatibilityTests --no-parallel`; `swift test --filter ReportFixtureValidationContractTests --no-parallel` | Remove `--jacktrip-hub-tcp-handshake`, handshake reports, and TCP handshake codec. |
| JT-05D | P1 | JackTrip | Add authentication/TLS behavior. | done | Added JackTrip auth response codes, authenticated TLS request/client-info frame encoding, credential length validation, redacted media-report auth state, and tests proving secrets are not persisted in reports. Measured TLS peer evidence remains required before any field-readiness or `PASS` claim. | `swift test --filter JackTripTCPHandshakeTests --no-parallel`; `swift test --filter JackTripCompatibilityTests --no-parallel`; `swift test --filter ReportFixtureValidationContractTests --no-parallel` | Restore auth/TLS to unsupported modes and remove authenticated frame codec/report state. |
| JT-06A | P2 | JackTrip | Add WebRTC mode if clean-room public evidence is sufficient. | done | Added WebRTC data-channel packet support using the same JackTrip packet bytes as UDP, plus 4-byte big-endian JSON signaling frame helpers for protocol-detect/offer/answer/candidate/hangup messages. Dependency-heavy ICE/DTLS networking remains outside `PASS` until measured peer evidence exists. | `swift test --filter JackTripCompatibilityTests --no-parallel` | Restore `webrtc` to unsupported modes and remove WebRTC data-channel/signaling frame helpers. |
| JT-06B | P2 | JackTrip | Add WebTransport mode if clean-room public evidence is sufficient. | done | Added WebTransport datagram packet support with QUIC varint quarter-stream ID prefix handling and the same JackTrip packet bytes as UDP after the prefix. msquic/HTTP3 live networking remains outside `PASS` until measured peer evidence exists. | `swift test --filter JackTripCompatibilityTests --no-parallel` | Restore `webtransport` to unsupported modes and remove WebTransport datagram framing helpers. |
| JT-06C | P3 | JackTrip | Add JamLink header mode. | done | Added native JackTrip `JAMLINK` header mode for representable 16-bit mono/stereo PCM, including compact stream-type bitfield encoding/decoding, sequence and timestamp fields, parser selection via `--jacktrip-header jamlink`, report propagation, and malformed/unrepresentable header rejection. Reference-peer evidence remains required before any interoperability or `PASS` claim. | `swift test --filter JackTripEmptyHeaderTests --no-parallel`; `swift test --filter JackTripCompatibilityTests --no-parallel` | Restore `jamlink-header` to unsupported modes and remove `--jacktrip-header jamlink` propagation. |
| JT-06D | P3 | JackTrip | Add plugin mode support or document a durable non-goal. | done | Added `--jacktrip-plugin audio-bridge` configuration and launch-plan propagation as an explicit plugin-host boundary over the native packet runtime. Plugin host loading remains external evidence and cannot satisfy live-device or peer `PASS` by itself. | `swift test --filter JackTripCompatibilityTests --no-parallel`; `bash scripts/verify-docs.sh` | Remove `--jacktrip-plugin` parsing and plugin launch-plan propagation. |
| JT-06E | P3 | JackTrip | Add empty-header mode if required by reference peers. | done | Added native JackTrip `EMPTY` header mode as raw planar PCM datagrams with explicit session-derived decode metadata, parser selection via `--jacktrip-header empty`, report propagation, and redundancy rejection because no DEFAULT header is available to delimit concatenated packets. Reference-peer evidence remains required before any interoperability or `PASS` claim. | `swift test --filter JackTripCompatibilityTests --no-parallel`; `swift test --filter ExternalConnectorSessionTests --no-parallel` | Restore `empty-header` to unsupported modes and remove `--jacktrip-header empty` propagation. |
| JT-06F | P3 | JackTrip | Add non-PCM audio mode support or document a durable non-goal. | done | Added `--jacktrip-payload-encoding opus-celt-low-delay` and an Open LoLa Opus extension envelope over the JackTrip packet model using the existing vendored Opus bridge. This is source-level non-PCM support and still requires measured peer evidence before any interoperability claim. | `swift test --filter JackTripCompatibilityTests --no-parallel`; `swift test --filter OpusCELTLowDelayCodecTests --no-parallel` | Remove `--jacktrip-payload-encoding` parsing and Opus extension packet helpers. |
| JT-07 | P0 | JackTrip | Add evidence-gated JackTrip `PASS` validation. | done | JackTrip media validation now allows `PASS` only when real-link transmission is true, runtime error is absent, all runtime `PASS` evidence classes are observed, and `missingEvidenceClassesForPass` is empty. Synthetic/incomplete PASS reports are rejected, including an invalid synthetic PASS fixture. A valid measured PASS artifact still waits for JT-03 field evidence. | `swift test --filter JackTripCompatibilityTests`; `swift test --filter ReportFixtureValidationContractTests`; `swift test --filter ExternalConnectorReportTests` | Restore unconditional `dryRunCannotPass` rejection for JackTrip media reports. |
| SH-01 | P1 | Shared | Keep public CLI, app, docs, scripts, fixtures, and release policy truthful. | done | CLI/session parsing, app availability, docs, scripts, schema inventory, and release policy remain aligned with bounded provider/sink evidence, JackTrip bit-depth support, and blocked reference-peer `PASS` boundaries. | `swift test --filter AppShellSlice05Tests`; `swift test --filter ExternalConnectorSessionTests`; `swift test --filter ExternalConnectorNmpPlanTests`; `swift test --filter ExternalConnectorNmpWorkflowTests`; `swift test --filter ReportSchemaInventoryTests`; `swift test --filter VerificationToolingPairScriptTests`; `bash scripts/verify-docs.sh`; `bash scripts/verify-release-hygiene.sh`; `git diff --check` | Revert public claim updates before reverting runtime slices. |

## Status Legend

- `open`: ready to plan or implement.
- `blocked on ...`: must not start until listed evidence or prior slice exists.
- `blocked on evidence`: needs public protocol evidence and a scope decision
  before implementation.
- `done`: implementation, tests, docs, and evidence are complete.

## Current Counts

| Priority | Open | Blocked | Done |
| --- | ---: | ---: | ---: |
| P0 | 0 | 0 | 3 |
| P1 | 0 | 2 | 7 |
| P2 | 0 | 0 | 12 |
| P3 | 0 | 0 | 5 |

Next slice: external reference-peer evidence capture. UG-03 and JT-03 remain
blocked only for measured interoperability evidence and final `PASS` claims.
Implementation-capable runtime rows must stay open when they can be built and
tested locally; skip-loud readiness reports must not be counted as measured
interoperability proof.
